- Unpacking (and maybe packing) of a GPAK doesn't seem to be working -- look into it.
- Ability to scroll through tab bar with scroll wheel.
- Dropdown tab list when there are more tabs that can be shown.

- The CSV map editor needs to be completely overhauled.
    - Want to check if the active node's LVL text is a properly formatted as tileset-level.
    - Seems to be an issue with custom node colours for people -- look into it.
        - Hard-code default colour values for the existing game areas.

- Go through and check that all features of the editor are working correctly.
- Do one final pass to make sure the source code is clean and organised.
